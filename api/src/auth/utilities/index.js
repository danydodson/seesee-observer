import {
  roles,
} from './roles'

import {
  setAuthToken,
  setVerifyEmailToken,
  setForgotPasswordToken,
} from './tokens'

import {
  setup,
  setPassword,
  verifyPassword,
  signToken,
  getRedirectUrl,
} from './utilities'

export {
  setup,
  setPassword,
  verifyPassword,
  signToken,
  getRedirectUrl,
  roles,
  setAuthToken,
  setVerifyEmailToken,
  setForgotPasswordToken,
}